from dummy-pungi import *

tree_variants = ["Client"]
pkgset_repos = {
    "i386": [
        "{{configdir}}/repo",
    ],
    "x86_64": [
        "{{configdir}}/repo",
    ],
    "s390x": [
        "{{configdir}}/repo",
    ],
}
